#include <stddef.h>
#include <signal.h>
#include <sys/ucontext.h>


#define ucontext(member)	offsetof (ucontext_t, member)
#define mcontext(member)	ucontext (uc_mcontext.member)
#define mreg(reg)		mcontext (gregs[REG_##reg])

#define oGS		mreg (GS)
#define oFS		mreg (FS)
#define oEDI		mreg (EDI)
#define oESI		mreg (ESI)
#define oEBP		mreg (EBP)
#define oESP		mreg (ESP)
#define oEBX		mreg (EBX)
#define oEDX		mreg (EDX)
#define oECX		mreg (ECX)
#define oEAX		mreg (EAX)
#define oEIP		mreg (EIP)

